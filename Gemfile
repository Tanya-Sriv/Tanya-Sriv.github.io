source "https://rubygems.org"
# The github-pages gem pins exactly what GitHub Pages runs in production,
# so a local `bundle exec jekyll serve` matches the deployed site.
gem "github-pages", group: :jekyll_plugins
